#all_product
	%ul
	- unless @all_products.nil?
		- for product in @all_products	
			%li.row
				%p= link_to image_tag(product.product_image.url(:small), class: "image_thumb"), product_path(:id => product.id)
				%p= "Price: $#{product.price}"
